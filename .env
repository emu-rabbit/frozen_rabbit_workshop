# Teamcraft data source branch.
# 'staging' = Teamcraft's active dev branch, receives data updates (incl. TW) first.
# 'master'  = Teamcraft's production branch, synced only when they deploy their site.
# Change this value and redeploy to switch all data sources at once.
VITE_TEAMCRAFT_BRANCH=staging
